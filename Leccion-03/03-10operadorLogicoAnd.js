// operador logico de and && (siginica y)
// Regresa TRUE solo si ambos operandos son verdaderos
let a = 5;
let valMin = 0;
let valMax = 10;

if (a >= valMin && a <= valMax){
    console.log(`Dentro de rango`);
}
else{
    console.log(`Fuera de rango`);
}