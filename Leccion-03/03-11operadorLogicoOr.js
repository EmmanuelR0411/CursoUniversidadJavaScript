// operador logico or || (significa o)
// Regresa TRUE si cualquier operando es verdadero

let vacaiones = false, diaDescanso = false;

if ( vacaiones || diaDescanso){
    console.log(`Puede asistir al juego del hijo`);
}
else {
    console.log(`El padre esta ocupado`);
}