// Como buena practica se recomienda usar la primer letra en ,ayuscula cuando se trata de un nombre compuesto
// Sintaxis correcta
let nombreCompleto = 'Pedro Juarez';
console.log(nombreCompleto);

// Declarar variables en una misma linea
let x, y;
x = 20, y = 30;
let z = x + y;
console.log(z);

// JS es sensible a mayusculas y minusculas, así que es diferente Variable de variable
// No es pueden declarar variables comenzando con numeros